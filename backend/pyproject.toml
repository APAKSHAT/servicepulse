[project]
name = "servicepulse"
version = "1.0.0"
description = "Uptime and latency monitoring service"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 99
target-version = "py311"
